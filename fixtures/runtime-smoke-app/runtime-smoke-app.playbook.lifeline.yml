name: runtime-smoke-app
archetype: node-web
repo: local-fixture
branch: main
port: 4310
env:
  mode: file
  file: .env.runtime
  requiredKeys:
    - PORT
    - SMOKE_TOKEN
deploy:
  strategy: rebuild
  workingDirectory: .
